NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
# Only for an intentional local production-bundle preview. Never set this in Amplify.
# ALLOW_LOCAL_API_BUILD=true

# Optional provider overrides; defaults use OSM standard tiles and the public OSRM car demo.
# NEXT_PUBLIC_MAP_TILE_URL=https://tile.openstreetmap.org/{z}/{x}/{y}.png
# NEXT_PUBLIC_ROUTING_BASE_URL=https://routing.openstreetmap.de/routed-car
